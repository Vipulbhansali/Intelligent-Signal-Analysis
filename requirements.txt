dill==0.3.5.1                   # Dill is a Python library for object serialization. It extends Python's pickle module for more complex data structures.
dnspython==2.2.1                # Dnspython is a DNS toolkit for Python. It provides functions for working with DNS, such as resolving domain names to IP addresses.
fastapi==0.78.0             
httptools==0.5.0                # used in conjunction with web frameworks for handling HTTP requests and responses
imblearn==0.0
mypy-boto3-s3==1.24.76          # Mypy-boto3-s3 is a type-checked wrapper for the AWS SDK (boto3) specifically for the S3 service. It enables static type checking for interactions with AWS S3 in a Python project 
pip-chill==1.0.1                # Pip-chill is a tool that displays the Python packages and their versions that are installed in your virtual environment
pymongo==4.2.0
python-dotenv==0.21.0
types-s3transfer==0.6.0.post4   # This package provides type hints (static typing information) for the s3transfer module. Type hints enhance code readability and enable static type checking.
uvicorn==0.18.3                 # Uvicorn is an ASGI (Asynchronous Server Gateway Interface) server that is used to run FastAPI applications and other ASGI-compatible frameworks.
watchfiles==0.17.0              # Watchfiles is a tool that monitors files for changes. It is commonly used during development to automatically trigger actions (like restarting a server) when files are modified
websockets==10.3                # Websockets is a library for adding WebSocket support to Python. It enables bidirectional communication between a server and clients over a single, long-lived connection.
wincertstore==0.2               # Wincertstore is a library for interacting with the Windows certificate store. It provides utilities for managing SSL certificates on Windows.
xgboost==1.6.2
pandas
PyYAML
numpy
scikit-learn
apache-airflow
-e .